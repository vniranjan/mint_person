import StatementsClient from "./_components/statements-client";

/**
 * Statements page — server component shell.
 *
 * Lists all uploaded statements and their processing status.
 * Compact upload drop zone at top for returning users.
 */
export default function StatementsPage() {
  return <StatementsClient />;
}
